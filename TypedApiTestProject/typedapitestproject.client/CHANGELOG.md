# Changelog

## 0.0.5

- Updated to the bundled `typedapi-client-helpers` 0.3.5 tarball.
- Added generated methods and browser tests for every filter-form generation mode.
- Added generated-source assertions for `buildFilterQuery`, `buildQuery`, regular fallbacks, required query values, and headers.
- Added browser tests for generic responses, nullability, discriminated unions, typed errors, callbacks, aborts, and parse errors.
