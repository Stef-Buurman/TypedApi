# Changelog

## 0.0.5

- Added the `FilterFormController` endpoint-generation matrix.
- Added method-level and parameter-level `[TypedApiFilterForm]` examples.
- Added required map query, pagination-priority, unmarked-control, mixed path/query, and header examples.
- Added delayed and malformed-JSON endpoints for client runtime behavior tests.
- Switched `TypedApi.Swagger` to the bundled local 0.3.2 source project.
